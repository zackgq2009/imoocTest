<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context-3.1.xsd
       ">

    <!-- 配置了配置文件 -->
    <context:property-placeholder location="jdbc.properties"/>

    <!-- 配置c3p0的连接池 -->
    <bean id="dataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
        <property name="driverClass" value="${jdbc.driver}"/>
        <property name="jdbcUrl" value="${jdbc.url}"/>
        <property name="user" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!-- 配置accountDAO这个bean -->
    <bean id="accountDAO" class="com.tsingkuo.webapp.springtransaction2.dao.AccountDAOImpl">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- 配置accountService这个bean -->
    <bean id="accountService" class="com.tsingkuo.webapp.springtransaction2.service.AccountServiceImpl">
        <property name="accountDAO" ref="accountDAO"/>
        <!--<property name="transactionTemplate" ref="transactionTemplate"/>-->
    </bean>

    <!--<bean name="dataSourceTransactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">-->
        <!--<property name="dataSource" ref="dataSource"/>-->
    <!--</bean>-->

    <!--<bean name="transactionTemplate" class="org.springframework.transaction.support.TransactionTemplate">-->
        <!--<property name="transactionManager" ref="dataSourceTransactionManager"/>-->
    <!--</bean>-->

    <bean class="org.springframework.jdbc.datasource.DataSourceTransactionManager" id="dataSourceTransactionManager">
        <property name="dataSource" ref="dataSource">
        </property></bean>

    <bean class="org.springframework.transaction.interceptor.TransactionProxyFactoryBean"
          id="transactionProxyFactoryBean">
        <property name="target" ref="accountService"/>
        <property name="transactionManager" ref="dataSourceTransactionManager"/>
        <property name="transactionAttributes">
            <props>
                <prop key="transferAccounts">
                    PROPAGATION_REQUIRED
                </prop>
            </props>
        </property>
    </bean>
</beans>